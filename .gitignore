# R project, history, etc.
.Rproj.user
.Rhistory
.RData
.Rprofile
# knitr caches and files.
*_cache/
# OSX Stuff
.gitconfig.local
.DS_Store*
.Trashes
.DS_Store
# R markdown stuff
*.log
course_end_message
course_welcome_message
Intro_and_questions

# site updates
plan/

# Build artefacts: generated by the pdf-out and code-out chunks in exercises.Rmd
# and exercise_solutions.Rmd, then copied into docs/ by render_site. Only the
# docs/ copy is committed, so these are not tracked twice.
# Leading slash matters: it anchors to the repo root and spares docs/exercises/
/exercises/
/exercise_solutions/
